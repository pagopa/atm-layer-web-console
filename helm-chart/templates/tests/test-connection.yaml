apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atm-layer-web-console.fullname" . }}-test-connection"
  labels:
    {{- include "atm-layer-web-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atm-layer-web-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never