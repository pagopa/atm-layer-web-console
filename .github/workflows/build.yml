name: Build everytime push or merge

on:
  push:
    branches:
      - '**'        # matches every branch

defaults:
  run:
    shell: bash

permissions: write-all

jobs:
  build:
    strategy:
      matrix:
        environment: [uat]
    name: "build"
    runs-on: ubuntu-latest
    environment: ${{ matrix.environment }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # - name: Use Node.js 20.9.0
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: 20.9.0

      # - name: Build app
      #   run: |
      #     npm install
      #     npm run build
##
      - name: Configure uat AWS Credentials
        uses: aws-actions/configure-aws-credentials@67fbcbb121271f7775d2e7715933280b06314838
        with:
          role-to-assume: ${{ secrets.IAM_ROLE }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Pull pre-release = v1.0.0 from uat S3
        run: |
          aws s3api get-object --bucket ${{ vars.BUCKET_NAME_ARTIFACTS }} --key v1.0.0.zip ./v1.0.0.zip

      - name: Configure prod AWS Credentials
        uses: aws-actions/configure-aws-credentials@67fbcbb121271f7775d2e7715933280b06314838
        with:
          role-to-assume: ${{ secrets.PROD_IAM_ROLE }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Push release on prod S3
        run: |
          aws s3 cp v1.0.0.zip s3://${{ vars.PROD_BUCKET_NAME_ARTIFACTS }}/
